package edu.groupname.compiler.common.token;

public enum TokenType {
    KEYWORD,
    IDENTIFIER,
    INTEGER_LITERAL,
    REAL_LITERAL,
    OPERATOR,
    DELIMITER,
    BOOLEAN_LITERAL,
    EOF,
    UNKNOWN
}

